version: 2
jobs:
  build:
    working_directory: /go/src/github.com/sample-hands-on-123/hello-go
    docker:
      - image: circleci/golang:1.9
    steps:
      - checkout
      - run: go test -v ./...